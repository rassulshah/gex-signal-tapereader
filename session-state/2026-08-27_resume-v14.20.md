# Resume note — v14.20

## Where the build is (v14.20)
**v14.20 built 2026-08-27 ~11:00 ET; awaiting install** (footer must say v14.20; v14.19 was NOT
separately installed — this carries all six ledger fixes plus the step-back). v14.20 = THE EXPORT
IS THREE KINGS ONLY (operator: "too many levels"): SPXW KING + SPY KING → EPU26, QQQ KING →
ENQU26. Panel face unchanged — the trim is the FILE only. All prior export mechanisms retained
(grammar/colours/frame-independence/latched crown/self-fetched SPY king/0DTE QQQ king/ratio
chains). ⚠ If the operator later wants the full node set back in the file, the v14.19 build of
irtBuildCsv is in git history — do not re-derive it.
Live checks pending after install (v14.19 items ride along, still unverified): 3-line file in IRT ·
posts clear of labels · live pill · crown latch · single RRUG · warm-up guard after next reload.

## Where the build was (v14.19)
**v14.19 built 2026-08-27 ~10:45 ET; awaiting install** (footer must say v14.19; supersedes
v14.18 — NOTE the operator may still be on v14.17 in Tampermonkey; 14.18's basis rung ships within
this). v14.19 = the six-item ledger build: label clearance (g3emt 86px) · LIVE pill (nowLive) ·
warm-up capture guard + persisted rr-heal clock · crown latch (kingLatchTick, 120s, day-keyed) ·
RUG/RRUG anchor-only · SPY derived 5-min persistence + guaranteed SPY KING from the self-fetched
book ("i must always have the spy and spxw king"). Ledger issues #1 (ROC rolling-vs-gated) still
PENDING an operator decision; #7b (SESSION_DAY never recomputes after back-fill) still OPEN —
the warm-up guard neutralises its worst effect but the underlying staleness is unfixed.
Live checks pending after install: posts clear of labels · pill tracks live price · KING label
stable through a crown flap (challenger shows polarity role + its 100%) · single RRUG · SPY KING
row present in EVERY export write · em/rr pins refuse to capture during warm-up after a reload.

## Where the build was (v14.18)
**v14.18 built 2026-08-27 ~10:15 ET; awaiting install** (footer must say v14.18). v14.17's
skylit-spot basis override almost never fired (SPXW feed too sparse: 4 payloads/10min — ALSO the
reason export SPY rows are absent in RTH; their gate is honest, the feed is slow). v14.18 adds the
rung that fires: SPX spot = SPY-feed spot / derived[SPXW].ratio (self-fetched, <=12s, all-Skylit),
spotSrc 'skylit-spy'. Live check pending: rail King within ~a point of Atlas's overlay and
tracking through a moving tape.

## Where the build was (v14.17)
**v14.17 built 2026-08-27 ~10:05 ET; awaiting install** (footer must say v14.17; supersedes
v14.16, installed + live-verified: EM re-pinned ±19.5 est-tagged, 6 rail posts incl King drew,
read sentence node-rich). v14.17 = the BASIS fix: ifLadder's dispScale/undScale denominator is now
Skylit's own live spot when fresh (2% sanity, spotSrc disclosed; IF spot = fallback) — the rail
was placing every node ~6 ES pts above Atlas because IF's delayed spot anchored the conversion.
Live check pending: rail King lands where Atlas draws it (~7668 at build time), nodes track Atlas
within a point through a moving tape.

## Where the build was (v14.16)
**v14.16 built 2026-08-27 ~09:45 ET; awaiting install** (footer must say v14.16; supersedes
v14.15, installed + live-verified: NQ rows from the Atlas QQQ 0DTE ladder confirmed). v14.16 =
ledger issue #5: EM sanity floor (EM_MIN_FRAC, schema v4 purge, capture-refusal + emHealed) after
the midnight expired-straddle pin produced a 5-pt band; band clip REMOVED from skPiles/emPilesIF
(operator mandate: "the band is a measuring stick, not an admission filter" — rail/NODES/ROC/
export now carry every >=thr node like Atlas); emRailBounds widens to hold the outermost pile
(price-driven over/under untouched). ⚠ OPEN LEDGER: session-state/ISSUES-NEXT-BUILD.md has issues
#1-#4 still PENDING decisions/mockups (ROC rolling-vs-gated, flag label collision, post-behind-
pill occlusion, price-pill staleness) — do NOT build them without operator sign-off per entry.
Live checks pending after install: EM band re-pins to a sane width at next capture (schema purge
forces re-take; during RTH the straddle is live so it should pin correctly within a render) ·
rail shows King + all >=20% nodes · NODES/ROC rows populate · export carries full SPXW block.

## Where the build was (v14.15)
**v14.15 built 2026-08-27 (just past midnight ET); awaiting install** (footer must say v14.15;
supersedes v14.14, installed + verified: RGB colours CONFIRMED on the operator's NQ chart —
purple/yellow correct after the byte-swap fix). v14.15 = **NQ rows from the Atlas QQQ ladder**
(tapeMap('QQQ'), front/0DTE, signed pct → polarity colour; feed fallback fromFeed⇒ABSENT) after
the operator caught the weekly-vs-Atlas mismatch ("use atlas as the source of truth"). NQ rows now
expire with the 0DTE book like the rail. ⚠ At this hour the SPXW rail is DOWN (midnight session
roll — "LEVELS UNAVAILABLE", no prior-session anchor): the file legitimately carries only IF + NQ;
the ES blocks return when the new day's book loads pre-market. Live checks pending: v14.15 footer ·
IRT NQ chart ≡ Atlas QQQ overlay (same strikes/%s) · morning: ES rail rows return + SPY diamonds
match Atlas during RTH + King yellow ~7670-area at the correct converted price.

## Where the build was (v14.14)
**v14.14 built 2026-08-26; awaiting install** (footer must say v14.14; supersedes v14.13, which WAS
installed + live-verified incl. live NQ ratio capture and the toggle surviving reload). v14.14 =
two operator-caught export defects: (1) **IRT PENCOLOR is RGB, not COLORREF** — every colour since
v11.4 was byte-swapped (yellow King rendered BLUE); irtColor now (r<<16)+(g<<8)+b. (2) **prices
were chart-frame-dependent** — a file written while Atlas showed the SPXW cash chart put raw SPX
prices on EPU26 (~15 pts low); rail + SPY-derived now use dispScale only when an ES chart is live,
else undScale×R.r. Live check pending: colours render yellow/purple/lighter/red/green/neutral in
IRT, and prices identical whether Atlas shows ES or SPXW.

## Where the build was (v14.13)
**v14.13 built 2026-08-26; awaiting install** (footer must say v14.13; supersedes v14.12, which WAS
installed and live-verified). v14.13 = `irtNqRatio()` — the QQQ→NQ ratio SELF-MEASURES on an NQ
Atlas chart via the existing futures machinery (FUT_FAMILY NQ→QQQ pairing; measured live r=41.1911
on NQ1 29270.25/QQQ 710.505 after the 41.9 guess drew lines ~480 pts off-screen). Chain: live
(persisted gpts_irt_nqratio_v1) → last-good ≤14d → manual settings field (now FALLBACK ONLY) →
NQ_RATIO const; `~` = not-live, no longer permanent. **An NQ chart is a RECOGNIZED pairing (records
into the QQQ book) — the GLD/USO chart-flip risk does NOT apply to NQ.** Live store already seeded
with 41.196 via the browser. v14.12 was CONFIRMED live before this: footer seen, new grammar/colour
rows in the real file, toggle persisted ON under the fixed loadCfg, 12-row write incl. 5 ENQU26.

## Where the build was (v14.12)
**v14.12 built 2026-08-26; awaiting install + live confirmation** (footer must say v14.12).
v14.12 = the export's operator-locked label grammar (SOURCE + ROLE + STRENGTH: `SPXW KING 100%` /
`SPY 51%` / `IF CW0`-`PW0`-`MAG0`-`MP0`; order SPXW→SPY→IF; colours: SPXW yellow/purple, SPY
lighter shades, IF red/green + neutral MAG/MP) + **NQ in the same file by default** (QQQ book →
ENQU26 rows, manual ratio 41.9 default, `~` always, stale⇒absent; settings row added) + **the
reload bug fixed** (loadCfg never merged o.irt — "Export levels" reverted to OFF on EVERY reload
since the v8 rewrite; now merged + token-asserted). **CQG SYMBOLOGY: EPU26 = ES, ENQU26 = NQ**
(operator's platform — see PROJECT-CONSTANTS "CQG SYMBOLOGY"). test_irt_export rewritten (56
asserts); em_band §40 labels updated; suite at the six known pre-v13.8 reds.
**Live checks pending after install:** footer v14.12 · D-SPY→`SPY` rows appear during RTH and
match the Atlas diamonds · ENQU26 rows appear · settings show the NQ row · toggle SURVIVES a
reload (the bug this build fixes).

# Prior resume note — v14.11

**A load must also read `session-state/PROJECT-CONSTANTS.md` — the landmines live there — and
`session-state/SKYLIT-FEEDS.md`, which since 2026-08-26 carries THREE mandatory layers: the feed +
derived semantics, the chart-settings map with the four required-posture rules, and the FULL
APPLICATION MAP (every page of app.skylit.ai, toured per the operator's directive "i need you to
know the entire application"). The Skylit Academy doctrine mirror is `skylit-docs/` (learn/ =
authoritative, per SOURCE-OF-TRUTH.md); the IN-APP Academy course text (Talon Prompt Guide, Reading
The Dark Pool Prints) is catalogued in the app map but NOT yet mirrored.**

## Where the build is
**v14.11 built 2026-08-26; awaiting install + live confirmation** (footer must say v14.11 —
supersedes v14.10, which was never installed). v14.11 = derived-scale semantics MEASURED AND FIXED
(rows arrive PRE-CONVERTED to host scale; ratio informational; raw-k primary, ratio fallback) +
**session-state/SKYLIT-FEEDS.md** created — THE feed reference (read it beside PROJECT-CONSTANTS
for any feed work). Derived % = % of the SOURCE's own King, all strikes ≥ nodeThresh (20), not
King-only; derived window follows the page's expiry selector (measured 7705: derived-week 100% vs
0DTE ladder 19% — window difference, not error). Previous:
v14.10 = the DERIVED diamonds in the export: observer keeps the SPXW feed (LASTSPXW) for its
derived[] (sibling books + ratio); rows exported as `D-SPY 43%` slate/dotted at source×ratio→ES,
own-King-normalised, thresholded, stale⇒absent. (Diamonds ≠ VEX — operator-corrected; they are the
Derived toggle's overlay.) v14.9 CONFIRMED live (footer seen, trimmed file verified in
lsFlexLevels: IF CR/Mag/PS/MP + KING/GK/ACC rows). Previous:
v14.9 = FlexLevels TRIMMED to the operator's exact spec: rail SPXW→ES nodes (+SUCC) and IF 0DTE
levels ONLY (IF window switched toFri→dte0); SPY-book roles / derived lanes / our CR-PS-FLIP /
NextStop / PBentry removed with ABSENCE asserts; either source exports alone, both down = null.
Previous: v14.8:
v14.8 = FlexLevels label/colour rework (operator-directed off the first live IRT chart): labels are
NODETYPE + %King only (no strike text — IRT prints the price), node lines in the panel's polarity
colours (yellow brake / purple accel, COLORREF), SUCC de-striked; export confirmed WORKING live —
writes FlexLevelsExport.csv into C:\\Users\\rassul\\InvestorRT\\rtx\\lsFlexLevels every 180s (18 rows,
how:file). The MULTI-MARKET settings build (ES/NQ/GC/CL, cadence pills, GLD/USO self-fetch, chart-
flip guard) is mocked and queued NEXT. Previous: v14.7:
v14.7 = ATTRACT: the tractor on the rail (dashed flow from price toward the ONE ≥2×-dominant magnet,
node-coloured, quiet when no dominance) + the `attract` feature (top pull recorded per bar,
toward/away outcome, dominance-gate question; rules.json = 72). Day-verified first: 77% toward
within 30m on 2026-08-25 (one day, flattered); rolls confirmed caught (66→7665 shelf, 23/22→7680/85
close wall); 7675 while draining broke 19/19 vs 7665 while growing held 74% — the ROC arbiter
measured. NEXT (approved, mockup pending): v14.8 FlexLevels multi-market export (ES/NQ/GC/CL from
SPX/QQQ/GLD/USO, per-pair ratio + contract symbol, cadence 1/3/5/15/30/60m, Drive folder via the
existing FS-Access flow) + phase-1 chart-flip guard (foreign chart symbols must PAUSE recording,
not corrupt STATE.SPY with GLD candles — verified corruption path via futMode fallback) + GLD/USO
in the background self-fetch. Previous:
v14.6 = FLAG-POSTS behind the pill (sqrt-height, King diamond, role names safe on top), the three
measured interaction states (breathe / DEFENDING sonar rings / ABANDONING fracture via
reactDefence), the pill's silhouette tip (green right = up, red left = down), rollsLive() gating
EVERY roll display on RTH (latch still records), and the AFTER HOURS rail (EM-expired chip, EL/EH
struck through, target/budget/lane retired). ⚠ SUITE FINDING: 28 test files never exited nonzero —
silent-red since forever; all fixed, stale asserts rewritten for the deliberate redesigns. Honest
green now = everything except the six pre-v13.8 reds. Live-DOM checks after install: posts behind
pill with visible tops/bottoms, King diamond, breathing post under price, tip on the pill's edge,
NO roll arrows after hours, amber AFTER HOURS chip post-close. Previous:
⚠ v14.4 SHIPPED HALF-WRITTEN: its multi-replace edit script failed on the last assert and saved
NOTHING, losing three of five fixes; the suite stayed green (no coverage on the lost changes) and
the operator caught it on the live face. v14.5 re-lands them (minors on the rail's expiry, %K above
bars, ROLL BIAS chip in the profile header) + the pill width:auto fix ("7703▼" was a 10px circle)
+ rail neighbour-label thinning (7655/7660 mashed). PROCESS RULE from this: multi-replace scripts
save nothing on any failure — verify tokens IN THE FILE after editing, and verify UI builds against
the LIVE DOM after install, not just the suite.
v14.4 = five operator-caught fixes on the live v14.3 face: minors bind to the rail's own expiry
(they vanished after the close when exp rolled), %K back ON TOP of bars, ROLL BIAS chip into the
profile header (NODES copy retired), the white dot became the PRICE PILL (rounded ES + last-bar
▲/▼, stretched still red, test_em_band geometry contract intact), floating wall flags clamp inside
the band. v14.3 (CONFIRMED live):
v14.3 = the in-bar badge system (operator codes S/R/SF/RF/TU/TD/H states, RU/RD rolls, CW/PW 0DTE
walls in the nearest bar, ▶ watch + aged dot at the base, FIT RULE: draw only while it fits, hover
always complete), the ROC matrix (5M/15M/60M rows replacing the strike labels under the bars), a
sqrt-spaced $ axis with gridlines in the left margin, price as a vertical line, legend row, bars
11→15px, band 48→72px. Delivery is now the gexdropNNN.zip + applygexNNN.bat pair (see
PROJECT-CONSTANTS "Delivery landmines"). NODES untouched — retires only after the operator has
lived with the profile; rolls then become between-bar connectors. Previous (v14.2, CONFIRMED
PUSHED via the fallback pair): v14.2 = the operator-approved profile rework: rail-frame
alignment (hidden EL/EH clones flank the drawing area), LINEAR fill within each outline, %KING as
the bar label (peak-% stays in fill + hover), axis in frameNum, TODAY'S-SPXW-EXPIRY filter at every
layer (peakTick, profile draw, snap.vend recording, seeding via the half-of-largest-strike decade
rule, peak store schema v2 discards polluted v1), ⓘ info icons (profile header / WALLS / NODES),
profile blank in replay. The optional IF OI-structure second row was DECLINED by the operator. v14.1 fixes three operator-caught v14.0 profile defects: all-grey
bars (emPiles has no `cls` — colours now from tradeNodes), off-grid x-positions (rail strikes now
use tradeNodes `es`, identical to the dots and the NODES list), and CW/PW read from the to-Friday
ladder instead of 0DTE (now `ifChain(...).dte0.lv`, suppression honoured). Companion unchanged at **1.13**. v13.9 (roll latch, gutter
connector, scale-pin self-heal) shipped earlier the same day and WAS confirmed installed by the
operator; v14.0 stacks on it.

v14.0 (full reasoning in CHANGELOG):
1. **GAMMA PROFILE under the rail** — `gammaProfileHtml`: bars on the rail's x-scale; outline = day
   peak, fill = now, hollow = closed intraday; rail strikes in role colours + %-of-peak label,
   non-rail strikes grey/thin; CW/PW flags at IF's CR/PS (only book with a call/put split —
   Skylit measured non-decomposable, |net|≡v); descriptive WALLS line; ES x-axis, price ▾.
2. **Day-peak tracker** — `PEAK`/`peakTick`/`peakOf`, `gpts_peak_v1`, day-keyed, bar-gated saves,
   replay-guarded, boot-seeded from today's `snap.vend` rows (test_peak_profile.js runs all of it).
3. **Rail roll arrows textless** — numbers + age in the arrowhead hover (14×12px padded target).
4. **esTick trims trailing .00** everywhere (7709, but 7706.50 and −22.75 keep decimals).
5. **rolllatch feature** records `below`/`destBelowConf` per destination + question
   `rolllatch_dest_below` (operator hypothesis: roll destination under price = support regardless
   of roll direction; F6 growth control).

## To verify live after install
- Footer v14.4; grey minors visible even after the expiry roll; %K sits ABOVE each bar; ROLL BIAS
  chip at the profile header's right; the rail shows the price PILL with the last-bar arrow.
- Prior v14.3 checks: badges INSIDE bars only (nothing above them but floating walls with no host bar);
  King bar: gold 100 + PW/state pills; a short bar drops badges but its hover tells everything;
  ROC matrix aligns under the bars; $ axis labels at sensible round values; white price line moves.
- Prior v14.2 checks still apply: King bar tallest AND gold; bars pixel-under their dots INCLUDING at the right edge;
  a 56% bar visibly 56% full; % labels are %KING (King shows 100% gold); axis reads like the rail
  ("7664"); no SPY/QQQ/far-expiry bars anywhere; ⓘ icons hover-open their explainers.
- CW/PW hover says "(0DTE)" and names the SPX strike; a missing PW on a call-heavy day is correct
  (suppression), not a bug.
- %-of-peak labels move as Skylit values move; a dissipating strike's fill visibly drops inside its
  outline.
- CW/PW flags present when the IF ladder is fresh; absent (not faked) when it errs.
- Roll arrows show NO text; hovering the arrowhead gives the full sentence with age.
- NODES prices read 7709 (no .00), ES row keeps 7706.50.
- Peaks survive a reload (seeded from recorder) — %s should NOT all jump to 100% after reinstall.

## ⚠ DATA CAVEATS FOR THE NIGHTLY REVIEW (2026-08-25)
**(2)** `snap.vend` rows recorded before v14.2 (all of today) may contain CROSS-BOOK rows — SPY
strikes (~765) and QQQ (~711) beside SPXW (~7665), whenever those ladders were open. Range-filter
vend to strikes ≥ half the day's largest before using it. Clean from v14.2 forward.
**(1) — unchanged from v13.9**
Records **before ~11:35 CT today**: band-relative fields (`emPct`, band edges) and `w.dist` are
skewed **≈ +18 ES points** (poisoned scale-pin, healed live). Vendor-verbatim fields (velocities,
sizes, `snap.vend`) and the roll ledger are unaffected.

## Operator taught this session (doctrine additions, measured where possible)
- Node model reconciliation: size = magnet strength; polarity = character on contact; rate-of-change
  = defended vs abandoned (the arbiter). A dissipating node is a DOOR; vacated strikes are air.
- Rolls create S/R **on the destination's side of price**, not by roll direction: morning roll-downs
  built 7665 support; afternoon roll-ups (13:33–14:00) stair-stepped 7675/7680 under rising price.
  Now enrolled as `rolllatch_dest_below` so the review scores it instead of us believing it.

## Open threads (top of queue first)
- **Live confirmation of v14.0** (above).
- **"GEX auto-pull" task removal** — user was given `schtasks /Delete /TN "GEX auto-pull" /F`
  (legacy 2-minute Drive-inbox watcher, idle since 08-22, popping cmd windows). Confirm it's gone.
  The daily 15:30 data-push task must STAY.
- **Six pre-existing red tests** (red on v13.8 before this session, verified vs origin/main):
  test_expiry_profile, test_node_map, test_sma_cont, test_tapeking (needs jsdom), test_trendbadge,
  test_v1126_process. Burn down next quiet session.
- CHANGELOG v12.6–v13.8 history never written; snapshots stopped at v11.90 before this session.
- `skylit-docs/FINDINGS.md` still does not exist (findings live as code comments F5–F8).
- %PK as a *voting* feature is deliberately NOT built — display + recording only, until
  `rolllatch_dest_below` / stuck-vs-gone come back measured.

## Learned this build
- Skylit's feed cannot produce a call wall (|net|≡v — measured via callPutProbe live); IF's CR/PS
  is the only walls source. The SpotGamma question decomposes: walls = OI book, dynamics = flow book.
- The repo got polluted by `git add -A` sweeping downloaded installers into mockups/ (v13.8 push) —
  the 30MB installer hang. Builder now excludes .bat and caps payloads at 6MB loudly; the v13.9+
  installers purge `mockups/install*.bat` on the user's machine.
- A pin against wobble is only as good as its capture (scale-pin poisoning, healed + self-heal
  shipped in v13.9).
