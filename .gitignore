node_modules/
_tmp
*.bak
*.zip
.DS_Store
Thumbs.dbtools/review-pull.log

# the self-extracting installer: 10k lines of base64 that bloated every diff.
# Superseded by the Drive auto-pull pipeline (tools/gex-pull.bat). Built on demand, not tracked.
install.bat
v10.js
tools/gex-pull.log
# versioned delivery copies of the installer — install.bat is the canonical one
install-v*.bat

# delivery artifacts - must never enter history (2026-08-25)
gexdrop*.zip
applygex*.bat
pushgex*.bat
install-v*.bat
installv*.bat

# python bytecode from the study tools
__pycache__/
*.pyc

# (v15.12) ⚠ STRAY ARTIFACT / BROWSER PAGES AT THE REPO ROOT. `git add -A` on his machine sweeps
# ANYTHING in the folder — that is how ~28MB of old installers reached mockups/ at v13.8, and on
# 2026-08-31 a downloaded Claude artifact page (67a24bf4-….html, 14KB) was committed to origin the
# same way. A UUID-named .html at the root is never project content.
/????????-????-????-????-????????????.html
/*.crdownload
