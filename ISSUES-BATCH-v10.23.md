# v10.23 BATCH FIX LIST (diagnosis only — NO code until user says start)
_Opened 2026-08-12 during live-market review on v10.22. Source of truth = the tape._

## *** GOVERNING FRAMEWORK — 'HOW TO READ & USE HEATSEEKER' 5-STEP (docs, 2026-08-12) — shapes the WHOLE app ***
USER MANDATE: this page should IMPACT HOW WE USE THE APP a lot. Implementation thesis: transform the panel from a set of independent READOUTS into a GUIDED, ANTICIPATORY WORKFLOW that ends in ONE posture statement ('anticipate where price is headed, don't react'). The 5 steps map onto our 3 modules + the regime/flow layer:
- STEP 1 IDENTIFY MAGNETS -> NODE MAP foundation. Docs: larger value=stronger magnet; mark only SIGNIFICANT nodes, DON'T CLUTTER. Validates our 'strongest-level headline + only the nodes price will hit' design. Impl: Node Map shows the FEW nodes that matter, not every strike.
- STEP 2 SPOT THE KING -> KING TRACKER as the DESTINATION. Docs: treat King as the PRIMARY SESSION TARGET; drive-off early / pin late. Impl: King badge states the SESSION BIAS ('destination <K> by close') not just location. = 'set end-of-session bias'.
  - USER DECISION 2026-08-12 (v10.25 mockup review): King = **EOD anchor ONLY**. DROP the EOW/swing-trade framing entirely PROJECT-WIDE (this is a 0DTE DAY-TRADING tool). Step 2 info-icon text + the posture King-as-destination line both say 'EOD settlement anchor / destination <K> by close', never 'EOD/EOW' and never 'swing'.
- STEP 3 DEFINE THE RANGE -> WHIPSAW regime + midpoint flag. Docs: fade EDGES, AVOID midpoints ('symmetry kills edge'). Impl: turn the regime into an INSTRUCTION — at an extreme say 'fade this edge'; mid-range say 'stand aside'.
- STEP 4 WATCH GATEKEEPERS -> GATEKEEPER detector + TRI-INDEX CONFLUENCE. Docs How-to-Read says 'agree or stand aside' (hard) BUT USER DECISION 2026-08-12: treat as a DESIRABLE CONFIDENCE BOOSTER, NOT a hard gate — ELEVATE confidence when SPY/QQQ/SPXW agree, NOTE (don't kill) when they diverge (matches Best Practices 'confluence = confidence'). early-day gatekeeper rejections = powerful reversals; failed test => reshuffle. DATA: SPY+QQQ+SPXW(+VIX) ALL in the DOM tape NOW — NO API key needed.
- STEP 5 MAP THE FLOW -> S/R IMBALANCE + rate-of-change, the app's THESIS. Docs: accumulation=strengthening, dissipation=weakening, reshuffle=new structure; 'play deflections to trade AHEAD of the reshuffle, on the side of DEALER INTENT not retail emotion.' Impl: flow layer must detect the reshuffle FORMING and state 'bias shifting <dir>, act ahead of it' — not merely report 'support fading'.
OUTPUT: every session the app walks Magnets -> King(destination) -> Range(fade/avoid) -> Gatekeeper(+tri-index agree?) -> Flow(reshuffle forming) => ONE posture line: 'bias = X, act at Y'. This anticipatory synthesis REPLACES the removed READ (Issue F) with something doc-grounded and decision-first. Log the posture + outcome in the effectiveness tracker.
NEW WORK ITEMS SURFACED: (J) tri-index confluence = CONFIDENCE BOOSTER not a gate (SPY/QQQ/SPXW agree => elevate; diverge => note, don't block); SPXW is IN THE TAPE (no API); VIX = bonus risk-on/off context. (K) King-as-destination session-bias framing; (L) regime-as-instruction (fade-edge/stand-aside); (M) flow 'reshuffle-forming / trade-ahead' read. All feed the single posture output.
DATA CORRECTION (project-wide, 2026-08-12): SPXW + QQQ + SPY + VIX are ALL live in the DOM tape. Any earlier note calling SPX/tri-index/topping-VEX 'API-gated' is WRONG. API key would only add velocity/node-class/historical polish, not the index data.
- (N) INFO ICON (user 2026-08-12): embed the 5-step 'How to Read & Use Heatseeker' framework AND our mapping of it (how THIS app implements each step) behind an INFO (i) icon in the panel — an in-app methodology reference the user can open. Keep it concise; the 5 steps + the one-line 'how we use it' per step.
- (O) ANALYSIS-TAB METHODOLOGY (user 2026-08-12): also use the 5-step framework as PART OF THE ANALYSIS — structure the Analysis tab around the 5 steps (Magnets/King/Range/Gatekeeper/Flow), score how well each step's read played out (per-step effectiveness), so the retrospective mirrors the live workflow and the LLM can give step-specific improvement recs.

## DOCS RE-READ — CORE CONCEPTS (docs.skylit.ai/core-concepts, 2026-08-12) — findings vs our build
- POLARITY CONFIRMED (settles the d/pos question): docs state each strike has a value POSITIVE or NEGATIVE, color-coded Positive=Green->Yellow (lower-vol, smooth) / Negative=Blue->Purple (higher-vol, wicky, can OVERSHOOT before reversing = MM trap). Matches feed d/pos. => our deflect-vs-accelerate verdict logic is doc-backed. BUT Key Principle: rank by ABSOLUTE VALUE, not sign/color; polarity is a MODIFIER (how price behaves on contact), not the ranker. Exception: Rug = the ARRANGEMENT of signs IS the pattern.
- GATEKEEPER (richer than we had): 'bouncers' between price and King; deflection points causing directional shifts. FAILED test at gatekeeper => MAP RESHUFFLE (precedes trend change). Early-day gatekeeper rejections = HIGH-probability reversals. => Node Map Gatekeeper flag should (a) anticipate reshuffle on a failed test, (b) weight early-session rejections higher.

### TOPPING / BOTTOMING DETECTOR (VEX, HIGHER-TIMEFRAME) — reverse-engineered from docs + 3 images (2026-08-12)
- DIFFERENT LAYER: all other detectors read INTRADAY GEX (one day's ladder). This reads VEX (vanna) across a MULTI-DAY EXPIRATION MATRIX (strikes x future dates) to spot HIGHER-TIMEFRAME trend shifts / market tops & bottoms in the sessions AHEAD. Docs: 'we look primarily at VEX positioning rather than GEX — reliable for where dealers will be positioned in sessions ahead.'
- GROUND TRUTH (topping, wk of 11/7/25, NetVEX matrix cols 11/03->11/07):
  * SPY: 690 'hard wall' (+VEX ~$795M/$753M across days), 685 strong GATEKEEPER (+$1,363,248K), price 682 BELOW both, LITTLE/NO upside accumulation above; downside stair-step 685->676->673-> massive NEG node 670 (-$4.2B) in later expiration.
  * QQQ: king 635, price 629, same downside stair-step across 11/03->11/07 cols toward 611/610.
  * OUTCOME: SPY->661, QQQ->598 (~3% peak-trough). Confirms.
- REVERSE-ENGINEERED RULE (code this): TOP when on the MULTI-DAY VEX matrix: (1) little/NO upside accumulation above price (upside nodes flat/absent across forward dates), (2) a GATEKEEPER wall capping price, (3) a DOWNSIDE STAIR-STEP of growing negative VEX nodes across FUTURE expiration columns, (4) stronger positioning building to the downside session-over-session. BOTTOM = mirror. INVALIDATION (docs, explicit): higher accumulation at higher strikes + dissipation at lower strikes + stronger positioning closer to the money.
- DATA / UNLOCK: ties DIRECTLY to the VEX-forward item already in DOCS-DERIVED-SPEC: feed hook ALREADY captures data_type=vanna into LASTVEX but it is STORED-NEVER-READ. Feed also fetches MULTIPLE dates= columns (seen in network: dates=2026-08-12..08-06). So the multi-day VEX matrix is AVAILABLE now. Build = (a) read LASTVEX, (b) persist a multi-day VEX snapshot (strikes x forward dates), (c) run the topping/bottoming rule on it. No API key needed (per DOCS-DERIVED-SPEC 'VEX — CORRECTION: available NOW').
- ARCHITECTURE: this is a 4TH, HIGHER-TIMEFRAME module sitting ABOVE the intraday 3-part app (King / Node Map / Signal Finder). It sets the multi-day BIAS that colors the intraday reads (e.g. topping bias => favor fading rallies / short setups intraday). Reuses Gatekeeper + stair-step(Trend) + accumulation primitives, but on VEX + multi-day.
- STATUS: reverse-engineered; BUILDABLE once VEX is wired (LASTVEX read + multi-day persist). Distinct pass from the intraday GEX detectors. Log topping/bottoming calls + forward outcome (did the HTF move play, magnitude) in the effectiveness tracker.

### RAINBOW ROAD DETECTOR (regime) — reverse-engineered from Patternpedia + 3 annotated examples (2026-08-12)
- PAGE DEF: MULTIPLE prominent nodes of BOTH positive and negative values, spread out ('rainbow'), with NO CLEAR RANGE => choppy, erratic, VAGUE positioning. Approach: AVOID trading; wait for a higher-prob setup.
- GROUND TRUTH:
  * Ex1 (60pt): yellow 6215 +$11.8M / 6220 +$7.5M adjacent to purple 6200 -$12.8M / 6185 -$7.1M — opposing forces interleaved, scattered.
  * Ex2 (dense 637-644): huge alternating — yellow 644 +$29M / 642 +$24M OVER a block of purple 641 -$118M, 640 -$143M, 638 -$122M, 637 -$103M. price INSIDE a dense block of opposing giants.
  * Ex3 (100pt): rapid sign alternation neg6305/pos6280/neg6260/pos6255/neg6250/pos6200; MIDDLE holds some of the LARGEST exposures (opposing $8.4M / -$20.4M adjacent).
- REVERSE-ENGINEERED RULE (code this): RAINBOW ROAD when MANY (>=~4) prominent nodes of BOTH polarities, INTERLEAVED, HIGH DISPERSION (spread wide and/or rapidly alternating sign), NO clean 2-edge range AND NO one-sided dominance. KEY TELL vs Whipsaw: the MIDDLE is FULL of strong opposing nodes (Whipsaw's middle is EMPTY). KEY TELL vs Trend: balanced both-sign mix, not one-sided. Verdict = STAND ASIDE (vague positioning, chop).
- FINALIZED 3-WAY REGIME CLASSIFIER (all from current node data — replaces v10.21 approximation): compute prominent-node set (|value| >= significance threshold), then: TREND = prominent mass concentrated ONE SIDE of price (skew ratio >= ~1.8x). WHIPSAW = ~2 dominant edges + LOW-VALUE GAP between them (bimodal, edge/middle >= ~2x). RAINBOW ROAD = residual: >=~4 prominent, BOTH signs interleaved, NO empty middle, high dispersion. Discriminator inputs: count of prominent nodes, dispersion/spread, sign-mix, empty-vs-full middle, one-sided-vs-two-sided. Log regime + which rule fired + forward outcome (did chop/trend/range play) in the effectiveness tracker; let it refine the thresholds.

### WHIPSAW DETECTOR (regime) — reverse-engineered from Patternpedia + 3 annotated examples (2026-08-12)
- PAGE DEF: price trades a WIDE RANGE whose EDGES are defined by AT LEAST TWO high-value nodes, with FEW prominent nodes IN BETWEEN. Trade = FADE THE EDGES; AVOID the middle (esp. 0DTE — theta + unstable price erode premium).
- GROUND TRUTH:
  * Ex1 (25pt): edges 6675 +$28.5M (yellow) / 6650 -$32.0M (purple); middle 6660/6665 ~$9-13M => edge/mid ~2.5-5x. price 6655 near lower edge.
  * Ex2 (65pt loose): top edge negative cluster 6545/6540 (-$27M/-$34M purple), bottom edge 6480 +$7.9M (yellow); middle ~$0.1-1.5M => edge/mid 10-30x. price 6500.
  * Ex3 (60pt, FOUR edges): 6480/6475 (+yellow ~$14-19M), 6460 -$9.9M, 6440 -$9.6M (purple), 6420 +$19.7M; interior ~$1.5-5M => ~2-4x. price 6455 IN THE DANGEROUS MIDDLE between two walls.
- REVERSE-ENGINEERED RULE (code this): WHIPSAW when >=2 high-|value| nodes form range EDGES (both sides of the range) AND interior nodes are SMALL vs edges (edge/middle dominance >= ~2x). Bimodal/multimodal shape: HEAVY-light-HEAVY (or heavy-light-heavy-light-heavy for 3-4 edge cases). Polarity IRRELEVANT to defining edges (high ABSOLUTE value regardless of sign). Verdict: fade edges / AVOID middle; flag when price is mid-range = danger.
- REGIME DISCRIMINATOR (now all 3 quantitatively separable): TREND = one-sided heavy skew (top-3 same-side >= ~1.8x other, King far, counter-side small). WHIPSAW = edges both sides + EMPTY middle (bimodal, edge/mid >= ~2x). RAINBOW ROAD = many heavy nodes SCATTERED, NO clear edges (everywhere-heavy; page not yet examined). => upgrade the v10.21 regime classifier from approximation to these exact geometric tests. 8/11 (our data) = Whipsaw, consistent.
- BUILDABLE NOW (value+geometry). Log regime label + which edges + whether price respected/faded them in the effectiveness tracker.

### RUG / REVERSE-RUG DETECTOR — reverse-engineered from Patternpedia + 2 annotated examples (2026-08-12). PROJECT NAMESAKE. Buildable NOW.
- PAGE DEF: a YELLOW (positive-gamma) node stacked ABOVE a PURPLE (negative-gamma) node with NO obvious floor below = Rug. Mechanism: yellow ceiling unwinds -> the negative-gamma node just below ACCELERATES the rejection ('gasoline on fire') -> stack of negative-gamma floors below (no positive floor) -> violent nosedive. Confirm bias: downside GROWING + little/zero UPSIDE accumulation; strongest with SPX/SPY/QQQ agreement + a catalyst.
- GROUND TRUTH (SPY close-up, every strike readable):
  * 674 +$6.3M (pos), 673 -$6.2M (neg), **672 +$17.4M (YELLOW ceiling)**, **671 -$48.0M (PURPLE core, price ► here)**, 670 -$18.1M, 669 -$18.4M, 668 -$10.7M, 667 -$2.8M, 666 -$1.0M, 665 -$4.1M (all NEG stack), 664 +$4.0M, 663 +$2.5M (tiny pos => NO significant floor).
  * QQQ panel same geometry: 608 +$13.2M (yellow) over 606 -$25.7M (purple), price 610 above => same rug across symbols.
- REVERSE-ENGINEERED RULE (code this): RUG when ALL: (1) a meaningful POSITIVE-gamma node (pos:true) near/above price = ceiling; (2) DIRECTLY BELOW it a STRONG NEGATIVE-gamma node (pos:false, large |value|) — the yellow-over-purple ADJACENCY is the core; (3) below that a STACK predominantly NEGATIVE-gamma (acceleration floors); (4) NO significant positive-gamma floor beneath the stack (largest positive-below << the negative cascade => 'no obvious floor'); (5) CONFIRM: downside build-rate growing + ~zero upside accumulation (reuse S/R Imbalance / build-rate). Optional tri-index agreement boosts confidence.
- REVERSE-RUG = bullish mirror: PURPLE ceiling above over a YELLOW floor below, no significant negative ceiling above => violent upside squeeze.
- DATA: EVERYTHING NEEDED ALREADY EXISTS. pos (from feed d) = yellow/purple per node (docs confirm polarity IS the pattern basis — vindicates the earlier user challenge). value=size, adjacency=geometry, 'no floor'=scan downside for a significant positive node, 'downside growing'=build-rate. => Rug is BUILDABLE NOW in Node Map v1 (NOT a later pass), pending the one-time verify that sign(pos/d) matches ladder yellow/purple.
- NODE MAP INTEGRATION: when a Rug is detected, flag 'RUG forming' on the Node Map + AMPLIFY the break-through verdict on the fading purple floors (targets = the lit negative-gamma downside nodes). Reverse-Rug flags a squeeze-up. Log detection + outcome (did the nosedive/squeeze play, MFE/MAE, tri-index state) in the effectiveness tracker.

### TREND DETECTOR (GEX-skew trend) — reverse-engineered from Patternpedia + 3 annotated examples (2026-08-12)
- PAGE DEF: price fixates on a King FAR from spot with SMALL counter-directional skew; as price trades toward it, the node above becomes the new King (stair-step). Mechanically: nodes AWAY from price FADE while nodes IN the trend direction INCREASE. Enter on PULLBACKS (stair-stepping); chase only if asymmetric R:R.
- 3 EXAMPLES (ground truth, top-3-abs each side):
  * Ex1 DOWN: price 6435, King 6400 BELOW (-$57.6M). below3 ~100M vs above3 ~47M => ~2.1:1 down-skew. target 6400.
  * Ex2 UP: price 6565, King 6580 ABOVE (+$17.7M, dual 6580/6585 yellow cluster). above3 ~44.6M vs below3 ~23.9M => ~1.86:1 up-skew. target 6580/6585.
  * Ex3 UP: price 6420, King 6430 ABOVE (+$27.0M). above3 ~56.8M vs below3 ~28.3M => ~2:1 up-skew. target 6430.
- REVERSE-ENGINEERED RULE (code this): TREND when (a) King is AWAY from spot, AND (b) SKEW RATIO = sum(top-3 |value| on King side) / sum(top-3 |value| opposite side) >= ~1.8-2.0, AND (c) counter-side is SMALL/fading. DIRECTION = toward the heavy-skew side; TARGET = King. DYNAMIC confirm (docs): trend-side node values INCREASING while counter-side FADING (we already compute build-rate => reuse). Threshold ~1.8x from the examples; make it a tunable const + let the effectiveness tracker refine it.
- KEY ARCHITECTURE INSIGHT: this is a GEX-STRUCTURE trend (where dealer exposure skews), NOT a PRICE trend. Our existing trend badge = 16/20 bars vs 50-SMA = PRICE trend. They are DISTINCT + COMPLEMENTARY and can DISAGREE (price chopping while node-skew is bullish IS a signal). DO NOT replace the SMA badge. ADD GEX-skew as its own read: it is the precise, quantified definition of the 'Trend' REGIME we already approximate in the v10.21 regime classifier (Trend/Whipsaw/Rainbow). Node Map already has both sides' node values => skew ratio is nearly free; when decisively one-sided, label the day Trend + direction + King target, and drive 'enter on pullbacks' guidance.
- BUILDABLE NOW (value+geometry). Log skew ratio + direction + whether the trend played to target in the effectiveness tracker.

### GATEKEEPER DETECTOR — reverse-engineered from Patternpedia + 3 annotated examples (2026-08-12)
- PAGE DEF: high-value node sitting BETWEEN price and FURTHER high-value nodes, preventing continuation; becomes key S/R. Approach: mark it, scale out as price approaches, play a REVERSAL there ESPECIALLY IF gatekeeper value FAR EXCEEDS the nodes beyond it.
- THE 3 EXAMPLES (ground truth):
  * Ex1 (price 6615): page says 6600 (NEG -$11.86M) gatekeeps price from the big POS King cluster BELOW (6575 +$37.9M king / 6570 +$19.5M). Gatekeeper = the intervening blocker on the path DOWN to the King, NOT the biggest node.
  * Ex2 (CVNA price 367.5): 360 (NEG -$417.9K) is 'between spot and the ultimate target 350' (King 350 +$840.8K POS). Gatekeeper = obstacle on the road to the King.
  * Ex3 (SPY price 663): King 664 (NEG -$417.2M) just above price stalls upside; page says COMPARE gatekeeper vs the SECOND-highest node (665 -$140.2M) => ratio ~3x = DOMINANT gatekeeper. Here gatekeeper ~= at the King.
- REVERSE-ENGINEERED RULE (code this): GATEKEEPER = the NEAREST high-|value| node lying BETWEEN current price and the larger node(s)/King beyond it, in the direction of the big nodes, that price must pass through first. SIGNIFICANCE = STRENGTH RATIO = |gatekeeper| / |next MAJOR node BEYOND it toward the target| (docs: 'compare vs the 2nd highest-value node' — NOT rigidly vs King). Ratio >>1 (Ex3 ~3x) => expect STALL/REVERSAL AT the gatekeeper rather than continuation.
- POLARITY IS NOT THE DEFINING TRAIT: gatekeepers appear POSITIVE and NEGATIVE across the 3 examples => ABSOLUTE VALUE ranks, sign only FLAVORS (confirms Core Concepts). Do NOT gate gatekeeper detection on polarity.
- FITS NODE MAP NATIVELY: it's exactly the 'between price and King, in the travel direction' structure the two-sided map already orders. Detector = mark the highest-|value| intervening node + its strength ratio + verdict (reversal if ratio high; feeds King DECOY DISCOUNT: King gapped behind a strong gatekeeper = less reachable). Add reshuffle-anticipation on failed test + early-session weight (from Core Concepts).
- BUILDABLE NOW (geometry+value only). Log gatekeeper strike + ratio + reject/break outcome in the effectiveness tracker.
- KING confirmed: early reach->drive-off / late->pin; deflection margin ~5-10pt SPX ('zones'); rolling ceilings(bearish)/floors(bullish). Matches King Tracker.
- NEW — NODE FRESHNESS / RETEST DECAY: 1st touch strongest / 2nd ~66% / 3rd+ ~33%; a node already 'delivered from' has weakened influence. => Node Map must TRACK TOUCH-COUNT per node and modulate bounce-verdict confidence (untouched floor >> thrice-tested). (LEARNING-SPEC already had this as TODO — now tie it into Node Map verdicts.)
- NEW — PRICE-DELIVERY REVERSION: after a bounce off a node, reversion back depends on the node's RATE OF CHANGE afterwards (growing=>likely return; decaying=>low prob). => input to 'will price come back to this level' + exit logic.
- NEW — AIR POCKETS: zones of low/small nodes price slides through; POLARITY sets violence (neg-gamma pocket=sharp, pos=mild). => Node Map should mark gaps BETWEEN levels as air pockets (there's already an 'airPocketSpans' fn ref at L2037/2092 — wire it in).
- NEW — MIDPOINTS: MM trap, ~1:1 R:R, avoid; fade the range EDGES. => flag when price is mid-range (ties to Whipsaw regime 'avoid the middle').
- EVENT CAVEATS confirmed: HEDGE NODES (event-day, far, slow, insurance; closer=>more influence), OPEX week (nodes weigh less, distortion), Robinhood POWER HOUR 3:30pm ET (forced flow, fakeouts near gatekeepers, accelerates near King, can reshuffle). => the session-phase/OPEX/event flags already on the roadmap; encode these concrete rules.
- ACTION: fold touch-count freshness + air-pocket marking + reversion-rate into Node Map (Issue I) verdicts; enrich Gatekeeper with reshuffle-anticipation + early-day weighting; keep polarity as MODIFIER not ranker. Log all as inputs the effectiveness-tracker records.

### CORE-CONCEPTS BEHAVIORAL RULES — STATUS MAP (user emphasized these 2026-08-12)
1. KING pin-late / drive-off-early / pull-back-later => PARTIAL. Have pin-vs-reach timing (early=drive-off, late=pin). MISSING the full session-phase ARC: 'early reach -> expect drive-off THEN likely return to magnet into close'. TODO: make King behavior TIME-OF-DAY conditioned (session phase) narrative, not just static at-King/pinned.
2. GATEKEEPER early rejection = high-prob reversal; FAILED test => RESHUFFLE precedes trend change => MISSING (no live gatekeeper yet). Build in Node Map v1: detect gatekeeper, weight EARLY-SESSION rejections higher, and on a failed test flag 'reshuffle likely -> trend change'.
3. REVERSION likelihood from delivered-from node's rate-of-change (grow=>likely return / decay=>low prob) => MISSING as a feature (ingredient=build-rate exists). Build: a 'will price return to <level>' read + exit signal, from post-delivery node ROC.
4. RAPID ACCUMULATION / RAPID UNWIND => ALREADY DETECTED (ACC_RAPID_ROC, fire/snow markers) but UNDERUSED. TODO: elevate to a first-class read ('magnet pulling in hard' / 'level suddenly weakening'), feed into Node Map verdicts + effectiveness log.
5. ROLLING CEILINGS/FLOORS (bearish/bullish thesis) => ALREADY DONE (kingRoll + King Path). Docs confirm. Keep; ensure it feeds the READ/regime.
6. AIR POCKETS => PARTIAL. airPocketSpans fn scaffolding exists (L2037/2092) but NOT surfaced. Build: mark air-pocket gaps between levels on the Node Map + POLARITY-TAG expected violence (neg-gamma pocket=sharp/violent slide, pos=mild). Uses the confirmed d/pos polarity.
- ALL SIX: record their state + outcome in the Node Map effectiveness tracker (I5) so each rule's predictive value is measured, not assumed.

## ARCHITECTURE (user framing 2026-08-12) — the app is now THREE parts
1) KING NODE TRACKER — directional anchor / magnet: King strike, drift, pin/reach, decoy discount, + new TREND BADGE (Issue G). Answers 'which way is price pulled, how strongly.'
2) NODE MAP (Issue I, reshaped PROJ) — the terrain: two-sided S/R map, building vs dissipating, bounce/pullback/break-through verdicts, strongest-level headline. Answers 'what will price meet, what will dealers do there.'
3) SIGNAL FINDER — trigger layer: currently the breakout-pullback (BO) tracker. Answers 'is there an actionable setup now.'
- HIERARCHY = the docs' decision stack: King (thesis) -> Node Map (confluence/context) -> Signal Finder (entry). Mirrors LEARNING-SPEC 'price action -> Heatseeker confluence -> asymmetric R:R'.

## PATTERN DETECTION ROADMAP (user Q 2026-08-12: can the Node Map ID Gatekeeper/Rug/etc.?)
- LAYER SPLIT: PER-LEVEL / local-arrangement patterns => NODE MAP (Issue I). WHOLE-BOARD regimes => King/Analysis layer (regime classifier already shipped v10.21).
- GATEKEEPER => NODE MAP, computable NOW (geometry only). Def: highest-value node BETWEEN price and King that blocks continuation. Strength ratio = gatekeeper val / node-beyond; >>1 => stall/reversal verdict at that level + feeds King DECOY DISCOUNT (King gapped behind strong gatekeeper = less reachable). Natural extension of the map's between-price-and-King ordering.
- CLUSTER (grouped heavy nodes -> pin/chop) & DOUBLE-STACK (adjacent heavy -> bounce) => NODE MAP, computable NOW (geometric).
- RUG / REVERSE-RUG => NODE MAP, LIKELY BUILDABLE NOW. **CORRECTION (user challenge 2026-08-12, verified in code): per-node POLARITY ALREADY EXISTS.** The FEED gives each node TWO separate fields d and net; net = recent change (build-rate), d = a per-node +/- SIGN stored as pos=(n.d>0) at extractWalls L365, derived L381, synth-midpoint L410 ('posMid' combines a.d/b.d signs), and recorder L575/L579. d is DISTINCT from the change => it is per-node gamma polarity (positive/yellow vs negative/purple, dealer gamma direction) on EVERY node, live — NOT the weak best-effort 'tp' field I cited earlier (that was one recorder branch only). => Rug detectable NOW: a pos=true (yellow) node ABOVE price stacked over pos=false (purple) floor(s) below with no strong positive floor beneath -> yellow ceiling over neg-gamma floors that accelerate on a break. Reverse-Rug = bullish mirror (neg stacked below a positive floor). Also unblocks the polarity MODIFIER for Node Map verdicts (deflect vs accelerate) and D3.
- VERIFY-ONCE CAVEAT (tape-truth, NOT a blocker): d==gamma-polarity is INFERRED from code usage + GEX conventions, not a printed field label. Before shipping Rug on it, do a 5-min confirmation that sign(d) matches the ladder yellow/purple coloring on a couple known nodes (a purple neg-gamma node below price should read d<0). If confirmed => Rug/Reverse-Rug ship in Node Map v1 alongside Gatekeeper/Cluster/Double-stack. If d turns out NOT to be polarity, fall back to capturing color from the ladder/ feed gamma payload.
- SUPERSEDES earlier 'D3 gamma-sign capture is a prerequisite' framing: capture already present pending the one-time verify. Update D3/polarity items accordingly.
- REGIMES (Whipsaw / Trend / Rainbow Road) => NOT the Node Map; whole-board, live in King/Analysis layer (v10.21 classifier + planned live-read surfacing). They COLOR how all 3 modules are read.
- MEASURED: because Issue I mandates effectiveness data capture, EACH pattern flag (Gatekeeper/Cluster/Double-stack/Rug) gets the SAME hit-rate-vs-baseline + MFE/MAE + n treatment and LLM-refinement loop as the verdicts. Log the pattern label + outcome from day one.
- SEQUENCING: Gatekeeper + Cluster + Double-stack can ship in the Node Map v1 (geometry). Rug waits on D3 polarity. Regimes already exist; surface later.


## I. PROJECTED S/R -> two-sided NODE MAP (strong-level detection + path/route + direction-aware + works against the King)
### I0. INTENT (user 2026-08-12)
- Reshape 'Projected Sup/Res' into a NODE MAP that answers: as price moves, what S/R will it encounter, and what will each level DO (pullback / continue / bounce / break-through). This is high-importance ('must be done right'). CAN BE REFINED OVER TIME (iterative; ship v1, improve with data).
- PURPOSE (user's framing — the WHY): it is a DEALER-POSITIONING MAP that guides TRADE DECISIONS — a map to the King AND a map, in trend, of WHERE TO LOOK FOR LONGS/SHORTS, WHERE TO ADD on pullbacks, and WHERE TO EXIT — based on where strong S/R is BUILDING vs DISSIPATING. Dealer buying/selling (accumulation/dissipation) informs what likely happens next: bounce, pullback-add opportunity, break-through/continuation, exit zone. Works on the way TO the King and AWAY from it.
- MUST ALSO KEEP the current PROJ's strength: surface the STRONGEST support + strongest resistance (heavyweight walls) so they never get buried in the list.
- MUST work when price moves AGAINST the King (away from the magnet), not only toward it.
- MOCKUP APPROVED (rev2, node_map_mockup_v10_23_rev2): two-sided price-anchored map + strongest ★ headline + per-level verdicts + direction emphasis + toward/against-King scenarios. 'This is the right idea.'
### I1. WHAT'S THERE NOW + GAPS
- Today: strongestAccumulator() (line 3567) returns ONE fastest-BUILDING floor + ONE ceiling in +-STRONGEST_BAND(6), ranked by build-rate net only; futureStructureSummary() has the richer per-node rows (state/htrend/absorb/role/peak) but PROJ only shows the 2 chips. Gaps: (a) single level, not a route; (b) hard-filters to label==='Building' so strong-but-FADING (break-through) levels are hidden; (c) no per-level expected-reaction verdict; (d) can point at a level BEYOND the King (e.g. 775 when King 773) that's not on the road; (e) implicitly assumes toward-King direction.
### I2. DECIDED MODEL (user)
- TWO-SIDED, PRICE-ANCHORED MAP (user final point): ALWAYS show levels on BOTH sides of price — resistance ABOVE and support BELOW — regardless of travel direction. A strong ceiling above = rally resistance; a strong floor below = dip support; never hide a side. Mark the KING wherever it sits (above/below, target vs magnet-behind).
- STRONG-LEVEL HEADLINE (Variant B shape): keep a compact 'strongest Sup + strongest Res' headline (preserves old PROJ's job) ABOVE the ordered ladder. 'Strongest' = BLEND of size(%King) + build-RATE + nearness (user pick), NOT nearest-only and NOT rate-only.
- PER-LEVEL VERDICT: tag each level Bounce / Pullback / Break-through (pending final confirm in mockup Q2). INCLUDE FADING levels tagged Break-through (continuation) — do NOT filter to builders only (fixes gap b).
- DIRECTION = LIGHT EMPHASIS, NOT A FILTER (agent decision, ratified by user's 'both sides always'): map always shows both sides IN FULL; use TREND STATE (new trend badge) as primary orientation + recent short-term momentum as tiebreaker to HIGHLIGHT the side price is moving toward (marker/ordering). On reversal nothing disappears — emphasis just shifts. Degrades gracefully.
- AGAINST-THE-KING CASE: when price moves away from the King, still show both sides; the King is marked as 'magnet behind (snapback/pull risk)'. (Do this within the two-sided map; user emphasized both-sides over editorializing, so keep the King-pull note subtle/secondary.)
- RANGE: not a fixed +-6 blind band — include the meaningful levels around price on both sides; ensure the King and the levels between price and King are always included, plus the strong wall on the OPPOSITE side even if past the near band (so a big far floor/ceiling still shows in the headline).
### I3. DEPENDENCIES / HONESTY
- Verdict quality depends on de-flickered build-rates (Issues D + H commit-on-bar-close) and correct King (v10.22, done). Build the map as a CONSUMER of those, not a new parallel engine — reuse futureStructureSummary rows + accumulationStateFor + adaptiveProxStrikes.
- BOUNCE vs BREAK-THROUGH sharpens massively WITH polarity (pos-gamma deflect=real bounce; neg-gamma accelerate=break-through / Rug). Ship size+build-rate version NOW; upgrade verdicts once the per-node gamma-sign capture (D3 follow-up) lands. Honest 'forming' when node history < min samples.
### I4. OPEN (from mockup)
- Q1 direction confirmed: Variant B (headline + path) shape, two-sided. Q2 verdict wording (Bounce/Pullback/Break-through) pending final ok. Q3 include fading = YES. Q4 range = both sides of price (superset of the mockup's up/down path).
### I5. EFFECTIVENESS TRACKING + DATA CAPTURE (user MANDATE 2026-08-12 — design in from the START)
- REQUIREMENT: capture the data needed to LATER measure the node map's predictive effectiveness and IMPROVE it with data + the LLM producing CONCRETE recommendations. Cannot measure what we didn't record — so the recorder must log node-map STATE + OUTCOMES from day one, even before any analysis is built.
- WHAT TO RECORD per bar / per node-map emission (extend the existing daily-data recorder that already persists GEX snaps to /GEX-Signal-Tapereader/daily-data/gex_YYYY-MM-DD.json):
  * For each mapped level at emit time: strike, side(above/below), %King(size), build-rate/state(building/steady/fading), distance-to-price, nearness weight, is-strongest(★) flag, King strike + price + direction/trend state + travel-emphasis side, and the VERDICT issued (Bounce/Pullback/Break-through) + a verdict-confidence/'forming' flag.
  * Polarity per node (once D3 capture lands) so later analysis can separate deflect vs accelerate outcomes.
- OUTCOME LABELING (forward, so effectiveness is computable): for each level+verdict, record what price ACTUALLY did on reaching/approaching it over forward horizons (reuse existing out5/out10 style fwd windows in the recorder): did it bounce / pull back then continue / break through? capture reached?(y/n), time-to-reach, MFE/MAE from the level, and whether the verdict was CORRECT. Tag session-phase + event flags (already in roadmap) so edges can be segmented.
- ANALYSIS (LATER, Analysis tab + LLM): per-verdict hit-rate vs baseline + MFE/MAE + n (honest 'insufficient' on low sample), broken down by: verdict type, side, size bucket, build vs fade, distance bucket, toward/against King, trend state, polarity (later), session phase. Then the LLM turns these pooled stats into CONCRETE, actionable recommendations to refine thresholds/weights/verdict rules (e.g. 'fading floors within 1 strike broke through 71% (n=48) => raise break-through confidence'; 'far strong ceilings bounced only 40% => downweight nearness'). This mirrors the existing A_* edge-stats + coherence approach.
- HONESTY: never fabricate an effectiveness number the sample can't support; show n + baseline on every stat; mark 'insufficient' until enough samples pool across days.
- IMPL NOTE: define the node-map log schema alongside the metric so v1 STARTS RECORDING immediately (recorder writes it even while the Analysis/LLM layer is still TODO). This is the data-for-improvement pipeline the user mandated.
### I6. STATUS
- MODEL DECIDED + MOCKUP APPROVED (rev2). Purpose expanded to a dealer-positioning trade-decision map (longs/shorts/adds/exits). Effectiveness-tracking + data-capture is a MANDATE and must be designed into v1's recorder. Polarity + full LLM-recommendation analysis are LATER passes but the DATA CAPTURE for them ships in v1. Verdict wording + ★/emphasis markers: pending final sign-off (Q2/Q3 of rev2 review). Ready to spec after wording sign-off.

## H. S/R NODE-LADDER badges flicker too fast (Steady/Building/Fading/ABSORB on each strike row)
- USER REPORT (2026-08-12): the per-node status badges in the S/R node ladder (accumBlock) change too fast to read.
- ROOT CAUSE (subtle, DIFFERENT from D's raw-compare): the LABEL logic in accumTrend() (3161-3196) is actually well-guarded (dip budget ACC_DROP_BUDGET, ACC_CONFIRM_DOWN consecutive downs, meaningfulDD). The flicker is UPSTREAM: history sampling (nodeHistory push, lines 890-899) correctly adds one point per 3m bar close BUT between closes it UPDATES THE LAST POINT IN PLACE with the LIVE tape (line 898). accumTrend then reads the WHOLE absSeq — including that live, still-forming last point — on EVERY render. So intrabar the last sample wiggles tick-to-tick, feeding downRun/fromPeak/slope, and the badge flips Steady<->Building<->Fading within a single unfinished 3m bar. The anti-noise guards were meant for CLOSED samples but are applied to a jittering live one.
- NOTE: line 55 comment already CLAIMS 'Building/Steady/Dissipating JUDGMENT are aligned to 3m bar closes' — the live-last-point read VIOLATES that stated intent. Real bug vs. spec.
- FIX INTENT (same family as D): COMMIT the badge label on 3m bar CLOSE and HOLD it between closes; do NOT recompute the verdict off the live in-progress sample every poll. Option: run accumTrend on the CLOSED portion of absSeq (exclude the live last point); allow ONLY a genuinely RAPID move (ACC_RAPID_ROC) to update intrabar so the rapid-accumulation magnet flag stays responsive. Result: each node badge changes AT MOST once per 3m bar => readable. ABSORB tag likewise commits on close.
- CONSISTENCY: makes ALL flicker fixes one pattern = BAR-CLOSE-COMMITTED STATE (D S/R Imbalance header + H node badges; G/trend already reads the 16/20 machine on closed bars). Unify via a single 'commit on CANDLE_S bucket change' helper reused by both.
- TEST: closed points flat but live last point oscillates -> node label does NOT change intrabar; changes exactly once when a new bar closes; a rapid ROC still flips promptly.
- DECISION (user 2026-08-12): SCOPE = JUST THE FLICKER — stabilize via commit-on-bar-close; LEAVE wording + visual style AS-IS (no re-vocab, no restyle). INTRABAR = hold the committed label steady between closes BUT allow a genuinely RAPID move (ACC_RAPID_ROC) to update immediately so real fast-accumulation still shows; ordinary tick-jitter cannot flip it. => implement accumTrend judgment on the CLOSED portion of absSeq (exclude live last point) for the steady label, with a rapid-override path that reads the live point only when recent ROC >= ACC_RAPID_ROC.
- DECISION (user 2026-08-12, mockup review): BADGE COLOR follows DIRECTIONAL MEANING, not the raw action. Rule: resistance-building = RED (bearish, ceiling strengthening); resistance-fading = GREEN (bullish, ceiling weakening); support-building = GREEN (bullish, floor strengthening); support-fading = RED (bearish, floor weakening); steady = GREY (neutral). i.e. 'Building' is green ONLY on support; on resistance it is red. Prior code colored by action alone (build=green/fade=red) => a resistance node building showed green, which is backwards. Implement as a meaningCls(side, action) helper so every ladder row derives color from side+action. NOTE: this is the ONE style change permitted under H's 'style AS-IS' scope — it's a correctness fix (bearish must not render green), not a restyle.
- STATUS: FULLY DECIDED. Fix = commit-on-bar-close, closed-portion judging, rapid-override live; wording untouched; badge COLOR corrected to directional-meaning rule (above). Ready for the batch dev pass. (Pairs with D; shares the bar-close-commit helper.)

## A. King PATH — price line MISSING from the axis (real bug) + label it with signed offset
- USER REPORT (2026-08-12): no price line on the King Path axis; it's supposed to be there.
- ROOT CAUSE (kingSparkline, lines 3666-3667): the dashed price line is drawn ONLY inside `if(px!=null && px>=yLo && px<=yHi){...}`. The y-range [yLo,yHi] is built from the KING STRIKES (3623), price folded in (3624), then only 15% padding added (3626); tight ranges get just a ±1 expansion (3625). When price has drifted ~1pt+ away from the King cluster, or the path is a single flat strike, price can land at/just outside the padded window => guard fails => LINE SILENTLY DROPPED. Also drops whenever px is momentarily null (stale feed) with no fallback.
- LIVE CONFIRM (2026-08-12): King 773 ($341,899K), price 771.88 => price sits BELOW the 773-775 King path strikes, right at the bottom edge => line missing. Matches the report.
- FIX INTENT: (1) GUARANTEE the axis always includes price with real margin (fold px in BEFORE padding AND ensure a min gap, e.g. >=1 strike of headroom beyond px), so the line always renders; if px is still beyond the range, CLAMP the line to the axis edge and mark it (small caret) rather than hide it. (2) NEVER hide on transient null px — use last-known price for the reference line, dimmed. (3) LABEL the line with BOTH the current PRICE VALUE and the SIGNED offset vs King (user pick 2026-08-12), e.g. '771.9 (-1.1)' — price value + '(sign offset)' where -=below King / +=above. tabular-nums, near the line's right end; keep legible against the dashed line.
- KING STRIKE LABEL ON PATH (user 2026-08-12): in addition to the spot-price line, LABEL THE KING STRIKE (e.g. '773') on the gold current-King dot at the right end of the staircase (small tabular-nums text in gold, offset so it doesn't overlap the dot or the price label). So the path shows BOTH: the King level (on the gold dot) and the spot price (on the dashed line). Price-line label stays at the RIGHT end near 'now' as in the mockup: '771.9 (-1.1)'.
- SIZE (user 2026-08-12): INCREASE the King Path area by ~33%. Current canvas kingSparkline() line 3611: W=236, H=84 -> H=112 (84*1.33). Width stays full-width responsive; bump padT/padB proportionally if needed so the taller canvas doesn't just add whitespace. The y-axis strike labels + x-axis time labels in kingBlock (3846-3855) should still align.
- SPACE BUDGET: the +33% height is AFFORDABLE because Issue F REMOVES the READ section above it — dev pass must size the panel as ONE layout (READ gone => reclaimed vertical room used by the taller King Path). Do A's resize and F's removal together; don't let them fight.
- TEST: assert the price <line> is present in the returned SVG for: price inside range; price 1.5 below the lowest King strike (clamped+caret, still present); single-flat-strike path; px=null (uses last-known, present+dimmed). Assert the label contains BOTH the price value AND the signed offset with correct sign. Assert H==112 (or the agreed value).
- STATUS: confirmed real bug + label(both) + resize(+33%). Ready for the batch dev pass.

## G. King HEADER redesign — left-justify King node + add a TREND BADGE on the right (user decision 2026-08-12)
- LAYOUT (user 2026-08-12, updated): header row = TREND BADGE on the LEFT, KING NODE on the RIGHT (flex space-between). (Earlier draft had King left/trend right — SWAPPED per user.) King badge itself unchanged (crown + strike / price + offset arrow — see Issue B for the arrow-position tweak). kingBlock() header is line 3791, currently justify-content:center.
- DATA SOURCE (already exists, zero new math): trendVerdict('SPY') returns {state, up, dn, dom, win, ma, slope}. Confirmation rule = 16/20 bars one side of SMA50 ± 0.25 ATR (TREND_WINDOW=20, TREND_DOM=16, TREND_BANDX=0.25). NOTE: dom = dominant-side count (dn when down-ish else up). Depends on the Issue-C continuous-SMA fix to be populated early.
- COUNT MEANING (user pick): DOMINANT side + direction. up-ish states show '↑ up/win'; down-ish show '↓ dn/win'. (i.e. ↑ = bars ABOVE SMA, ↓ = bars BELOW.)
- BADGE STYLE (user 2026-08-12): MATCH THE KING BADGE — same height + pill chrome (1.5px border, radius ~16px, card bg), STACKED TWO-LINE like the King node: TOP = state code (colored by trendColorOf), BOTTOM = bar-count supporting it (e.g. '↓ 16/20' + slope tick). So trend badge (left) and King node (right) read as a matched PAIR flanking the header. Border color = state color (green/red/amber/gray). Details tweakable later.
- FORMAT (user pick): compact CODE + count, now arranged as the two stacked rows above. State->code+count+color(trendColorOf):
  - up        -> 'UP '   + '↑ '+up+'/'+win   (green)
  - dn        -> 'DN '   + '↓ '+dn+'/'+win   (red)
  - up-broken -> 'UP-BRK '+ '↑ '+up+'/'+win  (amber)  // was up, lost 16/20, dn not yet confirmed
  - dn-broken -> 'DN-BRK '+ '↓ '+dn+'/'+win  (amber)
  - flat      -> 'SIDE'  + ' –'              (gray/sub)  // sideways / no trend (esp. between a break and the next trend)
  - na        -> 'warming up' (gray), NO numbers (user pick) — should rarely show once Issue C lands
- SLOPE TICK (user pick: YES) — PLACEMENT + COLOR (user 2026-08-12): slope from sign(slope): slope>0 -> '↗' rising, slope<0 -> '↘' falling, ~0 -> '→'. PLACEMENT: NOT trailing the count row — put it to the RIGHT of the stacked state+count, VERTICALLY CENTERED, exactly like the King node's offset arrow sits to the right of the strike/price stack. So structural parallel: King = [crown | strike/price stack | offset arrow]; Trend = [state/count stack | slope tick]. COLOR: DIRECTIONAL — green (↗ rising), red (↘ falling), gray (→ flat). e.g. 'DN / ↓16/20' stack + red '↘' centered right = 16/20 below a falling SMA.
- BAR-COUNT ARROW COLOR (user 2026-08-12): the ↑/↓ before the count is colored BY STATE (same as the top code): green UP, red DN, amber UP-BRK/DN-BRK/SIDE. (Matches the state code color; not a separate above/below hue.)
- TOOLTIP: spell it out — 'Trend: 16/20 bars vs SMA<period> (±0.25 ATR band). <N>/<win> <above|below>; SMA <rising|falling|flat> (slope <val>). Broken = dominance lost, opposite not yet confirmed.'
- IMPL NOTES: purely a render addition in kingBlock() header; reuse trendWordOf/trendColorOf (add a trendCodeOf for the compact codes) + slope sign. Keep period from CFG.trendMA. NO change to trend math.
- TEST: assert code+count+arrow+slope-tick strings for each of the 6 states from a synthetic trendVerdict; assert 'warming up' + no digits when state==='na'.
- STATUS: DECIDED (left-justify King + compact trend badge w/ dominant count + slope tick + warming-up). Ready for the batch dev pass.

## B. King BADGE — offset format: SIGN left, number, ARROW right (user decision 2026-08-12)
- FINAL FORMAT: '<sign><abs><arrow>' => '+1↑' (King ABOVE price) / '-1↓' (King BELOW price). At price (off==0): show nothing (unchanged).
- SEMANTICS: SIGN (+/-) = offset direction (+ above price, - below). ARROW (↑/↓) = SAME above/below, reinforcing the sign (↑ above, ↓ below). Sign and arrow ALWAYS AGREE. This is the OFFSET axis ONLY — NOT drift. Drift stays in kingVerdict / King Path, never the badge (keeps offset vs drift un-conflated).
- WHERE: line ~3774. Current: arrHtml = arr + Math.abs(off) (arrow LEFT, no sign). Change to: sign + Math.abs(off) + arr, where sign = off>0?'+':'-', arr = off>0?'↑':'↓'. Color unchanged (above=green longAccent, below=red shortAccent). Keep tabular-nums + tooltip; update tooltip text to match ('King is N strikes ABOVE/BELOW price').
- CONFIRMED this is the header King node badge (near the 773 crown), the arrHtml at 3774.
- STATUS: DECIDED (+1↑ / -1↓, sign left + arrow right, offset-only). Ready for the batch dev pass.

## C. SMA-50 — script filters candles to TODAY-only, so its SMA differs from Skylit's continuous SMA (and is blank until ~2.5h in)
- TWO distinct problems, both from convertFiberCandles() (lines 289-306):
  1. AVAILABILITY: line 298 `if(naiveDayStr(t)!==todayStr) continue;` DROPS every non-today candle; line 300 `if(so<openSec) continue;` drops pre-8:30 (RTH only). So closedCandles() holds only today's RTH 3m bars. smaVal() (428) needs 50 -> 150 min -> ~2.5h into the session before ANY SMA exists. Meanwhile trendVerdict() (477) needs mp+1=51 or returns state:'na' => the "trend N/A (need more bars)" in the READ. trendVerdict also RE-computes its own inline SMA (485-486), as does the breakout MA-filter trendOkFor() (445). Three consumers, all blind early.
  2. CORRECTNESS (user's key insight): Skylit's chart SMA-50 is CONTINUOUS (rolls across prior sessions' closing bars). The app's SMA is SESSION-RESET (today-only). So even AFTER 50 today-bars exist, the app's SMA value != the SMA line drawn on the chart. The trend/breakout filter is measuring price against a DIFFERENT line than the user sees. Silent divergence all day, not just early.
- ROOT DATA IS ALREADY IN HAND: readFiberCandles() (266-281) reads TradingView's raw `p.candles` array (memoizedProps) — the FULL continuous multi-day series. convertFiberCandles VOLUNTARILY discards the pre-today / pre-open bars. So "can't you get OHLC and calculate?" -> YES, the continuous OHLC is right there; we're filtering it out.
- FIX OPTIONS (user to pick):
  (A) COMPUTE continuous SMA from the raw p.candles (keep prior-session closes): keep a separate continuous close series for the SMA/trend math, while KEEPING today-only filtering for the intraday node/King logic that legitimately wants session scope. Matches the chart because same continuous inputs. Preferred: self-contained, no dependence on Skylit's rendered pixel value.
  (B) READ Skylit's rendered "SMA 50" value from the DOM (canvas legend ~771.0x, seen at ref_204/205) as authoritative MA, our computed as fallback. Tape-truth-consistent but depends on scraping a canvas legend token that can move/disappear.
  (C) Hybrid: compute continuous (A) as primary, cross-check against the rendered value (B) and flag if they diverge (mirrors the King $K cross-check pattern from v10.22).
- DECISION (user, 2026-08-12): OPTION A + one-time verification. Compute continuous SMA from raw p.candles (keep prior-session closes) for the SMA/trend/breakout math; KEEP today-only filtering for the intraday node/King logic. VERIFY ONCE that our continuous value matches Skylit's rendered SMA-50; if it diverges / doesn't make sense, FALL BACK to Skylit's rendered badge value. Keep period user-configurable (CFG.trendMA, default 50).
- IMPL NOTES for dev pass: (1) add a continuous close series (do NOT reuse the today-only closedCandles); (2) smaVal + trendVerdict inline SMA (485-486) + trendOkFor MA-filter (445) must all read the continuous series so all three agree; (3) one-shot sanity check vs rendered value at first full-window, log/flag on mismatch, use rendered as fallback; (4) unit test: continuous SMA present at session open (not 'na'), and equals a hand-computed continuous average.
- STATUS: DECIDED (A + verify/fallback). Ready for the batch dev pass.

## D. S/R IMBALANCE (RENAMED from 'S/R Bias' per user 2026-08-12) — reframe + de-flicker + polarity
### D0. RENAME + REFRAME (the core idea)
- RENAME the section to **S/R Imbalance**. Concept shift (user): the tradeable signal is NOT 'which side is bigger' (static) — it's the DIVERGENCE in RATE-OF-CHANGE between the two sides, NEAR PRICE. i.e. one side BUILDING while the other DISSIPATING = IMBALANCE that moves price. Canonical case: bear pullback -> resistance above GROWS while support floors below FADE -> imbalance says the pullback fails, price rolls down.
- USER DECISIONS: Q1 yes (measure divergence in rate-of-change, near price). Q2 = rate-of-change PRIMARY but WEIGHTED by wall size/nearness (a fade of a big NEAR wall counts more). Q4 = only count within the adaptive near-price window; a fade AT the in-play level weighted heaviest.
- BADGE WORDING (user 2026-08-12): NAME THE MECHANISM. Format: '<Direction> imbalance — <gaining side><strike> building, <fading side><strike> fading'. e.g. 'Bearish imbalance — resistance 772 building, support 769 fading'; bullish mirror 'Bullish imbalance — support 770 building, resistance 773 fading'. Neutral => 'Balanced'; low-sample => 'forming'. Show the specific STRIKES so the near-price relevance is visible. (Naming the mechanism, not a bare color/label.)
- NODE-LADDER BADGE COLOR (user 2026-08-12, mockup review) [shared with H]: color by DIRECTIONAL MEANING, not raw action. resistance-building=RED (bearish), resistance-fading=GREEN (bullish), support-building=GREEN (bullish), support-fading=RED (bearish), steady=GREY. 'Building' is green ONLY on support; on resistance it must be red. Use a meaningCls(side,action) helper. This corrects the old build=green/fade=red action-only coloring which rendered a building resistance (bearish) as green.
### D1. METRIC (build on EXISTING primitives — all present today)
- nodeBuildRate(sym,k) (line 1014) = signed %King change over window = per-node 'growing(+)/fading(-)'. accumulationStateFor = Building/Steady/Fading w/ hysteresis. prox=1/(1+dist) already weights nearness. adaptiveProxStrikes = the near-price window gate.
- NEW: IMBALANCE = proximity-weighted [ Σ(resistance-above build-rates) ] vs [ Σ(support-below build-rates) ], i.e. the DIVERGENCE of the two sides' DELTAS (not their static levels). Sign => direction (resistance gaining = bearish pressure / support gaining = bullish); magnitude => how lopsided. Weight each node by build-rate (primary) x size x nearness (Q2), gated to the adaptive window (Q4), fade AT in-play level weighted heaviest.
- KEEP a SEPARATE static 'who's bigger' internally if useful, but the BADGE reads the DYNAMIC imbalance. (Current srBattle collapses forces to a single who's-bigger scalar — that DESTROYS the divergence info; the reframe stops doing that.)
- HONEST DEGRADATION: build-rate is noisy with few samples/node -> require a min sample before asserting a STRONG imbalance; show 'forming' otherwise (tape-truth).
### D2. FLICKER FIX (this was the 'flicking back and forth' the user re-raised — ALREADY SOLVED here)
- ROOT CAUSE (old): srBattle() line 1069 set dom by a RAW per-tick compare sup.force>res.force with NO dead-band / NO memory; near-equal forces recompute every poll and flip the label every few seconds.
- FIX (user-approved): DEAD-BAND + HYSTERESIS + BAR-CLOSE CADENCE; crossover flags derived from the STABILIZED state (stability-first, fire later). Reframing to imbalance REINFORCES this: a rate-of-change DIVERGENCE must be SUSTAINED to register, so it's inherently more stable than a level compare.
- IMPL: (1) imbalance stays 'balanced/neutral' unless |imbalance| exceeds SRB_BAND (relative to typical scale, not abs guess). (2) asymmetric hysteresis: once a side leads, require the other to clear a WIDER flip threshold OR hold >=N bar-closes; 'balanced' entered freely. (3) evaluate/COMMIT on 3m bar close (CANDLE_S bucket), badge reads committed state so intra-bar jitter never shows. (4) crossover (pullback-high / bounce) computed from the committed imbalance, not raw. (5) keep per-render SRB_CACHE. (6) TEST: oscillating near-equal series -> no flip inside dead-band, flips only after wider threshold/held bars; crossover once per real flip.
### D3. POLARITY (user raised: 'positive will deflect' — layer AFTER we capture sign reliably)
- PHYSICS (docs): POSITIVE gamma (yellow) = dealers trade AGAINST movement => price DEFLECTED/pinned (stabilizing wall). NEGATIVE gamma (purple) = dealers trade WITH movement => price ACCELERATED/through (unstable, trap/overshoot). So polarity decides whether a growing wall DEFLECTS (pos) or ACCELERATES (neg) price. Rate-of-change = WHO is gaining; polarity = whether that gain deflects or accelerates. Orthogonal.
- INTENDED RULES (once sign is trusted): pos-gamma resistance building = REAL bearish deflection (counts full). neg-gamma 'resistance' near price = NOT a barrier -> down-weight/invert (break-through risk). pos-gamma support building = real hardening floor (bullish deflection). neg-gamma floor FADING = the RUG -> AMPLIFY the bearish imbalance (acceleration), not just remove support.
- DATA-HONESTY GATE (critical): we DON'T yet have trusted per-node gamma polarity. The parser's signed %King (line 841) is VALUE-CHANGE sign, NOT gamma color. Real polarity exists only as best-effort node.tp (line 1409 'best-effort'; DOCS-DERIVED-SPEC flagged 'needs clean capture, promote a reliable per-node gamma-sign field'). Feed DOES know gamma (line 188 data_type=gamma) and the ladder renders yellow/purple, so ground truth is on the page — we just must CAPTURE it cleanly first.
- SEQUENCING (user 2026-08-12 CONFIRMED): SHIP IMBALANCE (D0-D2) on clean magnitude+rate data NOW. Add POLARITY (D3) as a MODIFIER in a LATER pass, only AFTER promoting a trustworthy per-node gamma-sign (from feed gamma payload or ladder color). Do NOT gate the imbalance ship on polarity. => log D3 as a separate follow-up item for the polarity pass.
- STATUS: FULLY DECIDED. Rename 'S/R Bias' -> 'S/R Imbalance'. Metric = divergence of the two sides' build-RATES, weighted by size+nearness, gated to the near-price window, fade-at-in-play weighted heaviest, honest 'forming' on low sample. De-flicker via dead-band + hysteresis + bar-close cadence, stability-first crossovers. Badge NAMES THE MECHANISM with strikes. Polarity = later pass after clean gamma-sign capture. Ready for the batch dev pass.

## E. READ "King bearish" vs the other bearish labels — SAME source, verified.
- ALL THREE labels come from kingVerdict():
  - READ headline "King bearish" (line 4410): kingDir = kv.dir. Shown only when trend not up/dn/broken and kingDir!=0.
  - King PATH pill badge (line 3841 kv.word) + gray-text "◉ Bearish ..." (line 3858 kv.word): kingBlock calls kingVerdict ONCE at line 3811 and reuses kv for BOTH — they can never disagree with each other.
  - READ calls kingVerdict separately BUT with identical inputs (same tape kingK, same px, same kd.moves) — so in practice all three agree. Earlier 'two call sites might drift' worry DOWNGRADED (inputs identical). Optional tidy: compute kv once and share.
- HOW COMPUTED (kingVerdict, 3704-3741): score = driftTerm*1.6 + magnet*1.0, *conv (stability). DRIFT = King strike migration over session (up=bullish/down=bearish), PRIMARY (1.6x). MAGNET = offset King-vs-price (above=bull lean / below=bear lean), ~half weight. STABILITY scales conviction + names regime, no sign flip. => "King bearish" MOSTLY MEANS KING ROLLED DOWN this session, NOT 'below price'. (Today: King +1 ABOVE price = mild bull magnet, but downward drift outweighs => net Bearish.)
- KEY FINDING (bridges to F): the King PATH ALREADY reconciles offset vs drift correctly. Its verdict tooltip (lines 3827-3831) states drift and magnet SEPARATELY and appends, when they conflict, kv.mixed note: "drift and magnet disagree ... treat as mixed until they align" (3831). The badge arrow tooltip (3774) also correctly ties 'magnet pulling up/down' to the OFFSET only. So the King block is honest; ONLY the READ sentence-builder (F) conflates them.
- STATUS: EXPLAINED/VERIFIED (single source, drift-dominant). No standalone fix needed beyond F; optional: share one kv call. Fold into F.

## F. [SUPERSEDED] READ section — REMOVE ENTIRELY (user decision 2026-08-12)
- DECISION: remove the READ section rather than fix the wording. Rationale: it's the only place conflating offset vs drift, it duplicates King Path / S/R Bias / Trend, and it flickers/contradicts. Removing it makes Issue F moot.
- NEW TOP SECTION: the KING BADGE (kingBlock) becomes the top of the dashboard.
- IMPL (trivial, clean): delete the single call at render() line 5057 `html+=readBlock('SPY');`. kingBlock() is already the next line (5058) => King badge auto-promotes to top; NO other section reordered.
- SAFETY: readBlock is referenced ONLY at its def (4387) + that one call (5057) — self-contained. Leave the function defined as dead code (like structuralReadHtml) to avoid breaking stray refs, or delete later. Nothing else reads its output/state.
- FOLLOW-UP: Issue E's 'share one kv call' tidy is no longer needed for the READ (gone); King Path remains the authoritative King phrasing.
- STATUS: DECIDED (remove READ, King badge on top). Ready for the batch dev pass.

## F-OLD (kept for reference). READ logic bug — "King 773 (1 above) pulling down" is self-contradictory
- ROOT CAUSE: kingPhrase() (line 4414-4419) glues TWO different concepts into one clause:
  - "(1 above)" = MAGNET OFFSET (kingK-px = +1, King above price).
  - "pulling down" = kingDir (drift-dominant verdict; King has been ROLLING DOWN).
- kingVerdict already computes a `mixed` flag (line 3724: drift up + magnet down, or vice-versa) but the READ's kingPhrase() IGNORES mixed and just prints kingDir as "pulling up/down". So a King sitting ABOVE price (magnet pull UP) while ROLLING down (drift bearish) prints the contradiction "1 above ... pulling down."
- ALSO semantically off per docs: "magnet pull" should describe the OFFSET direction (above=pull up toward it), while the drift is a SEPARATE bearish structural signal. The word "pulling" is being attached to the drift, not the magnet.
- FIX INTENT: separate the two ideas in the sentence, e.g. "King 773, 1 above price (magnet pull up) but rolling down (bearish drift) — mixed." Use kv.mixed to emit an explicit mixed phrasing instead of a false single-direction claim. Reconcile wording with docs (magnet = offset; drift = migration).
- STATUS: confirmed logic/wording bug — highest priority of the set.

## CROSS-CUTTING
- Items E+F share kingVerdict; fix them together and add a unit test asserting: King above price + drift down => READ says "mixed", never "above ... pulling down".
- Item D wants a stability/hysteresis test.
- Consider a MOCKUP of the revised READ + King badge/path before coding (user requested mockup-first).
