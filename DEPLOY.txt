DEPLOY v10.44 — magnet frame: single column, Node Map rebuilt, data repository
==============================================================================
You already ran the installer if you are reading this from the repo. It:
  - placed v10.js, current\gex-signal-tapereader.user.js, changelog\, docs\, tests, tools\, data\
  - registered Windows scheduled task "GEX data push" (weekdays 15:30, run-if-missed)
  - committed + pushed

REMAINING (one time):
1. WAIT ~2 min (GitHub raw cache), then open + click Update in Tampermonkey:
   https://raw.githubusercontent.com/rassulshah/gex-signal-tapereader/main/current/gex-signal-tapereader.user.js
2. Hard-reload Atlas (Ctrl+Shift+R).
3. In the panel footer click the folder icon (📁) once -> choose C:\Dev\gex-signal-tapereader\data
   -> "Allow on every visit". From then on the day exports itself at 15:01 CT.

VERIFY: footer "feed v10.44 · rec ●" · ONE column: READ ▸ / Deflections / Node Map ·
Node Map identities Flr/Ceil/Mag, STATE Acm/Dec/Steady + %, ACTIVITY Pull/BOw/BO·FT/Defl/Push ·
-γ nodes purple · Range chip in the Node Map header.
