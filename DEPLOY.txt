DEPLOY v10.43 — projection rendering fixes + BO-tag fix + King Path move labels
===============================================================================
1. Extract this zip ANYWHERE (nested subfolder is fine).
2. Double-click install.bat (Windows warning: More info -> Run anyway).
   It copies everything into C:\Dev\gex-signal-tapereader (incl. docs\KING-FIELD-GUIDE.html).
3. GitHub Desktop -> Summary: "v10.43 rendering + BO tag + path labels" -> Commit -> Push origin.
4. WAIT ~2 MINUTES (GitHub raw cache).
5. Open + click Update:
   https://raw.githubusercontent.com/rassulshah/gex-signal-tapereader/main/current/gex-signal-tapereader.user.js
6. Hard-reload Atlas (Ctrl+Shift+R).

VERIFY: footer "feed v10.43" · projected chart focused (no black wedge, far rails
as ▲ edge tags) · BO chip shows only live progress (e.g. BO·FT) · King Path shows
price labels at significant moves · tiles no longer truncate.
