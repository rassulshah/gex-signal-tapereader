# IRT-vs-Skylit consistency log — 2026-09-24

## T2 10:30 CT — 10:43:02

```
=== check-gammaprofile: FAIL  (ASOF 67013) ===
  FAIL  STALE: ASOF 67013 is 966m old (panel not writing?)
  FAIL  missing/short row: KINGNOW (0 < 1)
  ok    King node 7749.50 @ 100% (flag ok)
  ok    top-5 nodes ordered by |pct|: [100.0, 57.0, 46.0, 35.0, 31.0]
  ok    outside RTH (ASOF 67013) — READ/EXPMODEL/body not required
  ok    DAYEXP has a body (close-open -12.43)
```

_Run in the cloud container (this desktop is win32 and exposes no `device_bash`) against the freshly-staged live GammaProfile.csv, same method as prior entries; `--now-so 38580` = the plan's h\*3600+m\*60 at 10:43 CT. **Dead-panel FAIL — the export has not written today.** The staged file is frozen at **2026-09-23 18:36:56 CT** (device-mtime) with `ASOF 67013` (= 18:36:53 CT); two stages ~2m apart (~10:40 and ~10:42 CT) returned **byte-identical** content (6657 B), the dead-panel signature — not a mid-write tear. At 10:43 CT — deep in RTH — the CSV is **~966m (~16h) stale**: the Skylit panel / IRT FlexLevels export has produced no GammaProfile write today. The whole lsFlexLevels export is frozen together (FlexLevelsExport.csv 18:36:56, GammaProfile-SPY.csv / audit.json 18:36:22 CT). **The GEX sync/git side is alive**, though: `tools/gex-sync.log` last wrote **10:40:02 CT today**, ~3m before this check — so the machine is on and the sync that pushes this log to GitHub is running; it is specifically the panel export feeding GammaProfile.csv that is down. The second FAIL (`missing KINGNOW row`) is a property of that frozen 18:36 CT after-hours snapshot, secondary to the staleness. The non-freshness structural checks that could still run pass — King node 7749.50 @ +100% (flag ok), top-5 ranks ordered, DAYEXP body -12.43 — so the last-written data is structurally sound; the failure is **liveness, not corruption**. (SPX→ES scale unchecked here: this snapshot carries no KINGNOW SPX row.) **Push notification sent per plan (exit 2).** Wants a hands-on look at whether Skylit / InvestorRT is actually running this morning. The eyes-on Skylit↔IRT render comparison still needs an attended run._

## T3 12:00 CT — 12:04:58

```
=== check-gammaprofile: FAIL  (ASOF 67013) ===
  FAIL  STALE: ASOF 67013 is 1047m old (panel not writing?)
  FAIL  missing/short row: KINGNOW (0 < 1)
  ok    King node 7749.50 @ 100% (flag ok)
  ok    top-5 nodes ordered by |pct|: [100.0, 57.0, 46.0, 35.0, 31.0]
  ok    outside RTH (ASOF 67013) — READ/EXPMODEL/body not required
  ok    DAYEXP has a body (close-open -12.43)
```

_Cloud-container run (win32 desktop exposes no `device_bash`); `--now-so 43440` = 12:04 CT vs freshly-staged live GammaProfile.csv. **Dead-panel FAIL continues** — same frozen 2026-09-23 18:36 CT snapshot (ASOF 67013) as T2, now ~1047m (~17.5h) stale; no GammaProfile write today. Non-freshness structural checks still pass → liveness failure, not corruption. Push sent (exit 2)._
