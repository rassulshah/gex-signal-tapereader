# IRT-vs-Skylit consistency log — 2026-09-22

## T2 10:30 CT — 10:39:56

```
=== check-gammaprofile: WARN  (ASOF 38258) ===
  warn  EXPMODEL basis "em-open60" but 127m into RTH suggests "open60"
  ok    fresh: ASOF 38258, 82s old
  ok    all required rows present (92 STRIKE lines)
  ok    King node 7893.25 @ -100% (flag ok)
  ok    SPX→ES scale ok: 1.00846 (strike 7827 → ES 7893.18; front-month basis +66.2 pts)
  ok    top-5 nodes ordered by |pct|: [100.0, 93.0, 76.0, 52.0, 11.0]
  ok    READ row present: HOD,75,76,700,IN
  ok    DAYEXP has a body (close-open -10.03)
```

_Run in the cloud container (this desktop is win32 and exposes no `device_bash`) against the freshly-staged live GammaProfile.csv — ASOF 38258 = 10:37:38 CT, 82s old at read (fresh); `--now-so 38340` = the plan's h\*3600+m\*60 at 10:39 CT (the floored minute lands after the panel write, so no STALE mis-fire). Exit 1 = WARN, 0 FAILs. The single warn is the recurring benign `em-` basis label-prefix — the panel writes `em-open60` where the validator expects bare `open60`; the actual basis (open60) is correct for 127m into RTH, same warn as 09-18 and 09-21 T2/T3. King internally consistent (node 7893.25 @ -100%, flag ok); SPX→ES scale OK (1.00846, +66.2 pts Dec front-month basis); 92 STRIKE lines, READ present, DAYEXP body -10.03. Push notification sent per plan (exit 1). Nothing to action; the eyes-on Skylit↔IRT render comparison still needs an attended run._

## T3 12:00 CT — 12:12:35

```
=== check-gammaprofile: FAIL  (ASOF 42834) ===
  FAIL  STALE: ASOF 42834 is 18m old (panel not writing?)
  warn  EXPMODEL basis "em-open60" but 203m into RTH suggests "open60"
  ok    King node 7821.50 @ -100% (flag ok)
  ok    SPX→ES scale ok: 1.00851 (strike 7755 → ES 7820.96; front-month basis +66.0 pts)
  ok    top-5 nodes ordered by |pct|: [100.0, 66.0, 55.0, 48.0, 25.0]
  ok    READ row present: HOD,82,89,675,IN
  ok    DAYEXP has a body (close-open -10.09)
```

_Run in the cloud container (this desktop is win32 and exposes no `device_bash`), against the freshly-staged live GammaProfile.csv — same method as the T2 entry above. The CSV's device mtime is 11:53:55 CT and its ASOF is 42834 (11:53:54 CT); a re-stage + re-run returned an identical mtime and byte count, so this is a real staleness, not a staging artifact: the Skylit panel stopped writing GammaProfile.csv at ~11:53:54 CT, ~18m before this 12:12:35 CT check (10m threshold) => **FAIL STALE (exit 2)**. It is not a validator problem either — every content check on the last-written snapshot still passes (King flag, SPX->ES front-month scale, node-rank order, READ row, DAYEXP body). The single warn is the recurring benign `em-open60` basis label-prefix the validator reads as non-bare `open60`, as at prior checkpoints. Push notification sent per plan (exit 2). Action: confirm the Skylit panel / IRT feed is still writing GammaProfile.csv — no fresh write since 11:53:54 CT. The eyes-on Skylit<->IRT render comparison still needs an attended run._
